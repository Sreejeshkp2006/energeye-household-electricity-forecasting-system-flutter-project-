rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Basic authenticated access for debugging
      allow read, write: if request.auth != null;

      match /devices/{deviceId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
